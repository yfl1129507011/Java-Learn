<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fenlon.dao.BlogMapper">
    <insert id="add" parameterType="blog">
        insert into blog (title, author, create_time, views) values
        (#{title}, #{author}, #{createTime}, #{views})
    </insert>

    <select id="getListIf" parameterType="map" resultType="blog">
        select * from blog
        <where>
            <if test="title != null">
                title = #{title}
            </if>
            <if test="author != null">
                author = #{author}
            </if>
        </where>
    </select>

    <select id="getListChoose" parameterType="map" resultType="blog">
        select * from blog
        <where>
            <choose>
                <when test="title != null">
                    title = #{title}
                </when>
                <when test="author != null">
                    author = #{author}
                </when>
                <!--<otherwise>
                    and views = #{views}
                </otherwise>-->
            </choose>
        </where>
    </select>

    <update id="update" parameterType="map">
        update blog
        <set>
            <if test="title != null">
                title = #{title}
            </if>
            <if test="author != null">
                author = #{author}
            </if>
        </set>
        where id = #{id}
    </update>
</mapper>